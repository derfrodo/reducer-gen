export const use{{featureName.pascalCase}}ReducerContext: () => I{{featureName.pascalCase}}ReducerContext = () => {
    return React.useContext<I{{featureName.pascalCase}}ReducerContext>({{featureName.pascalCase}}ReducerContext);
};

export const use{{featureName.pascalCase}}ReducerContextState: () => IState{{featureName.pascalCase}}ReducerContext = () => {
    return React.useContext<IState{{featureName.pascalCase}}ReducerContext>(State{{featureName.pascalCase}}ReducerContext);
};

export const use{{featureName.pascalCase}}ReducerContextDispatch: () => IDispatch{{featureName.pascalCase}}ReducerContext = () => {
    return React.useContext<IDispatch{{featureName.pascalCase}}ReducerContext>(Dispatch{{featureName.pascalCase}}ReducerContext);
};
