import {{#if state.hasStateAsDefaultExport}}{{state.stateName}}{{else}}{ {{state.stateName}} }{{/if}} from "./state";

export const {{state.getDefaultStateMethodName}} = (): {{state.stateName}} => ({
{{#each state.properties}}    {{name}}: {{initialValue}},
{{/each}}
});

export default {{state.getDefaultStateMethodName}};
