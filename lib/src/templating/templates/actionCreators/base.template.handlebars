{{#each state.importsWithAdditionalLevel}}
{{this}}
{{/each}}

import {{reducerActions.mainReducerActionsName}} from "./../reducerActions/{{reducerActions.mainReducerActionsName}}";
import { {{actions.mainActionsEnumName}} as actions } from "./../actions/{{actions.mainActionsEnumName}}";

export const {{actionCreators.baseActionCreatorsName}} = {
{{#each state.properties}}
    set{{namePascalCase}}: (next{{namePascalCase}}: {{type}}): {{mainActionsEnumName}} => (
        {
            type: actions.{{baseActionEnumValue}},
            next: next{{namePascalCase}},
        }),
{{/each}}
}

export default {{actionCreators.baseActionCreatorsName}};
